[package]
name = "prod-code-gateway"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[lib]
name = "prod_code_gateway"
path = "src/lib.rs"

[[bin]]
name = "prod-code-server"
path = "src/main.rs"

[dependencies]
prod-code-protocol = { path = "../prod-code-protocol" }
prod-code-engine-rust = { path = "../prod-code-engine-rust" }
prod-code-engine-go = { path = "../prod-code-engine-go" }
prod-code-engine-generic = { path = "../prod-code-engine-generic" }
anyhow.workspace = true
clap.workspace = true
tokio.workspace = true
tokio-util.workspace = true
futures-util.workspace = true
serde.workspace = true
rapidfire = "0.2"
serde_json.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true
libc = "0.2"
# Dense intent search (roadmap 8.4): a sentence-embedding model run in process. ONNX Runtime is
# loaded at run time (`load-dynamic`), not linked: the prebuilt archive `ort` would download needs
# glibc 2.38 (#220). The model and the library are optional and live outside the repository.
ort = { version = "2.0.0-rc.13", default-features = false, features = ["std", "ndarray", "tracing", "load-dynamic", "api-22"] }
tokenizers = { version = "0.21", default-features = false, features = ["onig"] }
ndarray = "0.17"

[dev-dependencies]
tempfile.workspace = true
# The client side, so the live test drives the gateway the way an agent does. No cycle: the
# MCP crate knows nothing about the gateway.
prod-code-mcp = { path = "../prod-code-mcp" }
libc = "0.2"


